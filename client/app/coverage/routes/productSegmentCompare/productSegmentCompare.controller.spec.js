'use strict';

describe('Component: ProductSegmentCompareComponent', function () {

  // load the controller's module
  beforeEach(module('amxApp'));

  var ProductSegmentCompareComponent;

  // Initialize the controller and a mock scope
  // beforeEach(inject(function ($componentController) {
  //   ProductSegmentCompareComponent = $componentController('productSegmentCompare', {});
  // }));

  it('should ...', function () {
    expect(1).to.equal(1);
  });
});
